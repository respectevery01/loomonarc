{
  "name": "loom",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "deploy": "astro build && wrangler pages deploy dist --project-name=loom"
  },
  "dependencies": {
    "@reown/appkit": "^1.8.23",
    "@reown/appkit-adapter-ethers": "^1.8.23",
    "astro": "^5.0.0",
    "ethers": "^6.17.0"
  }
}
